kind: Service
apiVersion: v1
metadata:
  name: nginx 
spec: 
  selector:
    project: expense
    component: backend
    environment:
  ports:
  - protocol: TCP
    port: 80
    targetport: 80
---
kind: Pod
apiVersion: v1
metadata:
  name: backend
  labels:
    project: expense
    component: backend
    environment:
spec:
  containers: 
    - name: nginx
      image: nginx
 

