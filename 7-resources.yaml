apiVersion: v1
kind: Pod
metadata:
  name: resources
spec:
  containers:
    - name: nginx
      image: nginx
      resources:
        # soft limit
        requests:
          cpu: "102m"
          memory: "256Mi"
        # hard limit
        limits:
          cpu: "122m"
          memory: "512Mi"